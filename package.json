{
  "private": true,
  "scripts": {
    "format": "purs-tidy format-in-place stories/**/*.purs src/**/*.purs test/**/*.purs",
    "postinstall": "spago install",
    "server": "live-server ./public",
    "storybook": "spago bundle-app -m Stories.Main -p stories/**/*.purs --to ./public/app.js"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "purescript": "^0.14.5",
    "purs-tidy": "^0.7.0",
    "spago": "^0.20.3"
  }
}
